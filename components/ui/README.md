# Shared UI components

Place reusable, presentational components here (e.g. `Button`, `Input`, `Card`, `Modal`) to keep the design system in one place. Page-specific or feature components stay in `components/`.
